import {examples} from '@/assets/assets'
import Image from 'next/image'
import React from 'react'

const Examples = () => {
    return (
        <div className='py-14'>
            <main className='w-11/12 mx-auto'>
                <div className="flex items-center flex-col">
                    <h1 className='tag text-lg'>Examples</h1>
                    <p className='text-3xl md:text-4xl lg:text-[55px] font-header font-medium '>All generated by our AI</p>
                </div>
                <main className=' flex  mt-10 gap-8 flex-wrap justify-center '>
                    {examples.map((image,index) => (
                        <Image draggable={false} key={index} src={image.image} className=' rounded-md pt-2 ring-1 ring-neutral-300 shadow-md' width={250} height={500} alt={image.alt} />
                    ))}
                </main>
            </main>
        </div>
    )
}

export default Examples
